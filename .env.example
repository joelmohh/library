# Server Configuration
PORT=3000

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/library

# Session Secret (generate a random secret key)
SESSION_SECRET=your_session_secret_here_change_in_production

# JWT Secret for password reset tokens (generate a random secret key)
JWT_SECRET=your_jwt_secret_here_change_in_production

#Populate db data
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

REGULAR_USER_PASSWORD=user123
REGULAR_USER_EMAIL=student@example.com



# SMTP Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_here
SMTP_FROM_EMAIL=your_email@gmail.com
SMTP_FROM_NAME=Library System

# Application URL (used in email templates)
APP_URL=http://localhost:3000

# Cron Job Configuration
# Check for overdue lendings every day at 9:00 AM
CRON_OVERDUE_CHECK=0 9 * * *
# Send reminder 3 days before due date
LENDING_REMINDER_DAYS=3